<h1>Collected Records for <%= @study.title %></h1>

<% if @study.records.empty? %>
  <h2>No data collection events have been sent out yet.</h2>
<% else %>
  <% @study.records.each do |record| %>
    <li>Time Sent: <%= record.created_at %></li>
  <% end %>
<% end %>
