<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Hello <%= @participant.first_name %>,</h1>

    <% unless @record.notes.empty? %>
    <p>A note from the study administrator: <%= @record.notes %></p>
    <% end %>

    <p>Please follow this link:
      <a href="<%= new_record_participant_response_url(@record, @participant)%>">Enter Response</a>
      </p>
  </body>
</html>
